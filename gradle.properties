org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# Fabric Properties
  # check these on https://fabricmc.net/use
  minecraft_version=1.15
  yarn_mappings=1.15+build.1:v2
  loader_version=0.7.2+build.174

# Mod Properties
  group=com.minelittlepony
  displayname=HDSkins
  authors=Mumfrey, Killjoy1221, Sollace
  description=Separate skin server that also supports HD skins. Access via button on the main menu.

# === READ THIS === #
# When making a release, change release to 'RELEASE'.
# After committing and tagging, ALWAYS set it back to 'SNAPSHOT' and increment the version.
# Then commit and push again.
  version=6.1
  release=SNAPSHOT

# Dependencies
  modmenu_version=1.8.+
  http_client_version=4.3.2
  kirin_version=1.5-1.15
